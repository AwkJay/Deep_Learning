# Week 1 Quiz 1: Understanding of AI, ML, and Deep Learning

---

## Questions

1. What distinguishes Artificial Intelligence (AI) from traditional programming?
- a) AI cannot handle complex tasks, unlike traditional programming
- b) AI relies on explicit rules, while traditional programming learns from data
- c) AI uses data-driven learning, while traditional programming follows fixed instructions
- d) AI is only used for robotics

2. Which of the following best describes the "No Free Lunch" in Machine Learning?
- a) Training ML models is always free of cost
- b) All algorithms converge to the same solution
- c) No single ML model works best for all problems
- d) Supervised learning is always superior to unsupervised learning

3. Why is feature engineering important in ML?
- a) It eliminates the need for model training
- b) It only applies to Deep Learning
- c) It reduces the need for data
- d) It helps the model extract meaningful patterns from raw data

4. What is the key difference between classification and regression in supervised learning?
- a) Classification requires unlabeled data
- b) Classification predicts discrete labels, while regression predicts continuous values
- c) Regression is only used in reinforcement learning
- d) Classification cannot use neural networks

5. Which of the following is a key challenge in unsupervised learning?
- a) Inability to handle large datasets
- b) Overfitting due to too many labels
- c) Requires human intervention for every prediction
- d) Lack of labeled data for training

6. What is the role of a loss function in training a neural network?
- a) It measures the difference between predictions and true values
- b) It visualizes training data
- c) It initializes random weights
- d) It prevents overfitting by deleting data

7. Why is the ReLU activation function preferred in Deep Learning?
- a) It outputs probabilities between 0 and 1
- b) It is linear across all values
- c) It avoids the vanishing gradient problem better than sigmoid/tanh
- d) It only works in convolutional networks

8. What is the purpose of dropout in a neural network?
- a) To increase model complexity
- b) To randomly deactivate neurons during training to prevent overfitting
- c) To speed up inference time
- d) To reduce the number of input features

9. Which of the following is an example of reinforcement learning?
- a) Grouping customers into segments
- b) A robot learning to walk through trial and error
- c) Classifying emails as spam or not spam
- d) Predicting stock prices using historical data

10. What does "bias-variance tradeoff" refer to in ML?
- a) Balancing underfitting (high bias) and overfitting (high variance)
- b) The difference between training and testing accuracy
- c) The ethical bias in datasets
- d) The computational cost of algorithms

11. Why is batch normalization used in Deep Learning?
- a) To reduce the number of layers in a network
- b) To increase the learning rate indefinitely
- c) To eliminate the need for activation functions
- d) To stabilize and accelerate training by normalizing layer inputs

12. What is the primary advantage of transfer learning?
- a) Leveraging pre-trained models to save time and computational resources
- b) Replacing the need for backpropagation
- c) Training models without any data
- d) Making models invariant to all types of noise

13. Which of these is a limitation of Deep Learning?
- a) Requires large amounts of labeled data
- b) Only works with tabular data
- c) Cannot be used for real-time applications
- d) Does not require GPUs for training

14. What is the purpose of a confusion matrix in classification?
- a) To initialize model weights
- b) To visualize neural network architecture
- c) To evaluate model performance by comparing predicted vs. actual labels
- d) To preprocess input data

15. Why is gradient descent used in training ML models?
- a) To eliminate the need for training data
- b) To increase the loss for better generalization
- c) To make models deterministic
- d) To iteratively minimize the loss function by adjusting parameters

16. What is the key idea behind a convolutional neural network (CNN)?
- a) Only processing sequential data like text
- b) Working exclusively with 1D vectors
- c) Using filters to detect spatial hierarchies in data (e.g., images)
- d) Ignoring local patterns in input data

17. What does "generalization" mean in ML?
- a) A model’s ability to perform well on unseen data
- b) Using only synthetic data for training
- c) Memorizing training data perfectly
- d) Reducing the number of layers in a neural network

18. Which technique helps mitigate overfitting?
- a) Removing all validation data
- b) Increasing model complexity
- c) Using a smaller training dataset
- d) Regularization (e.g., L1/L2)

19. What is the role of an optimizer (e.g., Adam, SGD) in Deep Learning?
- a) Reducing dataset size
- b) Generating synthetic training data
- c) Adjusting model weights to minimize loss
- d) Visualizing feature maps

20. How does a generative adversarial network (GAN) work?
- a) It cannot generate images or text
- b) It eliminates the need for loss functions
- c) It only performs supervised classification
- d) A generator creates fake data, while a discriminator tries to detect it

## Correct Answers

1. c
2. c
3. d
4. b
5. d
6. a
7. c
8. b
9. b
10. a
11. d
12. a
13. a
14. c
15. d
16. c
17. a
18. d
19. c
20. d
