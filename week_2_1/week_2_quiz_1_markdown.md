# Week 2 Quiz 1: Basics of CNNs

---

## Questions

1. What is the primary function of a convolutional layer in CNNs?
- a) Fully connect neurons
- b) Flatten the input
- c) Extract spatial features from input
- d) Perform dimensionality reduction

2. In CNNs, what does a filter (or kernel) do?
- a) Reduces overfitting
- b) Normalizes the data
- c) Detects patterns like edges or textures
- d) Adds non-linearity

3. Which of the following operations is typically applied after convolution?
- a) Pooling
- b) All of the above
- c) Activation Function
- d) Batch Normalization

4. What does the term 'stride' refer to in convolutional layers?
- a) Number of pooling operations
- b) Number of steps the filter moves across input
- c) Number of filters used
- d) Size of the filter

5. What is the typical function of pooling layers in CNNs?
- a) To reduce spatial dimensions
- b) To add bias
- c) To apply filters
- d) To perform matrix multiplication

6. Which type of pooling takes the largest value in a patch?
- a) Min pooling
- b) Sum pooling
- c) Max pooling
- d) Average pooling

7. What happens when the stride in a convolutional layer is increased?
- a) Output size increases
- b) Output size decreases
- c) Output becomes denser
- d) No effect on output size

8. Which of the following best describes padding in CNNs?
- a) Prevents underfitting
- b) Increases filter size
- c) Reduces overfitting
- d) Maintains spatial size of output

9. ReLU is often used in CNNs because it:
- a) Introduces non-linearity
- b) Both B and C
- c) Helps reduce dimensions
- d) Prevents vanishing gradient

10. How does a CNN differ from a fully connected neural network?
- a) CNNs do not use activation functions
- b) CNNs share weights across space
- c) CNNs don’t require training
- d) CNNs use only linear layers

11. A 3x3 kernel applied to a 5x5 image without padding results in an output size of:
- a) 3x3
- b) 4x4
- c) 5x5
- d) 1x1

12. Which of the following is not a hyperparameter of the convolutional layer?
- a) Input image size
- b) Activation function
- c) Stride
- d) Filter size

13. If we use 'same' padding, the output feature map will have:
- a) Smaller size than input
- b) Always square shape
- c) Larger size than input
- d) Same size as input

14. Filters in early CNN layers usually learn to detect:
- a) High-level patterns
- b) Faces
- c) Edges and textures
- d) Objects

15. The output of one filter after convolution is called a:
- a) Weight matrix
- b) Padding map
- c) Feature map
- d) Layer

16. Which of these reduces model parameters in CNNs?
- a) Weight sharing in convolution
- b) Using smaller kernel sizes
- c) All of the above
- d) Using pooling layers

17. Why are CNNs preferred for image tasks over MLPs?
- a) They use more activation functions
- b) They capture spatial hierarchy
- c) They require less training data
- d) They have more layers

18. Which layer typically comes at the end of a CNN for classification tasks?
- a) Convolutional layer
- b) Pooling layer
- c) Normalization layer
- d) Fully connected layer

19. The output depth of a convolutional layer is determined by:
- a) Stride
- b) Kernel size
- c) Padding
- d) Number of filters

20. What is the primary reason for using pooling in CNNs?
- a) Downsample and extract dominant features
- b) Improve accuracy
- c) Increase computation
- d) Normalize data






## Correct Answers

1. c
2. c
3. b
4. b
5. a
6. c
7. b
8. d
9. a
10. b
11. a
12. a
13. d
14. c
15. c
16. c
17. b
18. d
19. d
20. a

