# Week 2 Quiz 4: Self-attention and Transformer Models

---

## Questions

1. What is the main purpose of self-attention in transformer models?
- a) Reducing vocabulary size
- b) Capturing relationships between all tokens in a sequence
- c) Assigning position to tokens
- d) Replacing activation functions

2. What is the key innovation introduced in the Transformer architecture?
- a) Max pooling layers
- b) Convolutional filters
- c) Attention mechanism without recurrence
- d) Recurrence

3. What is used to provide positional information in transformers?
- a) Time delay units
- b) Recurrent cells
- c) Positional encoding
- d) Convolutional kernels

4. Which of the following is not a component of the transformer encoder?
- a) Multi-head attention
- b) Decoder layer
- c) Positional encoding
- d) Feed-forward network

5. What does the “multi-head” in multi-head attention refer to?
- a) Applying dropout at multiple places
- b) Using multiple GPUs
- c) Adding multiple encoders
- d) Splitting the input into several attention layers

6. Why is Layer Normalization used in transformers?
- a) To normalize attention scores
- b) To stabilize and accelerate training
- c) To reduce dimensions
- d) To increase vocabulary size

7. What type of neural network layer is completely removed in transformer models compared to RNNs?
- a) Dense layers
- b) Convolutional layers
- c) Recurrent layers
- d) Attention layers

8. Which of the following tasks can be effectively solved using transformers?
- a) Image recognition only
- b) Arithmetic operations only
- c) Binary classification only
- d) Text summarization and translation

9. What is the function of the decoder in a transformer model?
- a) Translate input text into target output
- b) Encode input text
- c) Generate embeddings
- d) Normalize inputs

10. Which dataset is commonly used for training translation models?
- a) WMT (Workshop on Machine Translation)
- b) MNIST
- c) COCO
- d) CIFAR-10

11. What is the role of the softmax function in attention?
- a) Increasing sequence length
- b) Compressing sequences
- c) Creating embeddings
- d) Normalizing attention weights

12. In text summarization using transformers, what is typically the input?
- a) Token embeddings
- b) Label data only
- c) A long document or article
- d) A short phrase

13. How are input sequences processed in a transformer model?
- a) In parallel using self-attention
- b) In a loop one token at a time
- c) By max pooling each token
- d) Using convolutional windows

14. Which library is commonly used to implement transformers in Python?
- a) Transformers by Hugging Face
- b) OpenCV
- c) PIL
- d) NLTK

15. What are the three main vectors computed in the attention mechanism?
- a) Head, weight, scale
- b) Query, Key, Value
- c) Input, Output, Position
- d) Token, embedding, label

16. What is the output of the encoder in a transformer?
- a) Loss values
- b) Encoded contextual representations
- c) Attention heads only
- d) Predictions

17. Which transformer variant is most widely used for text summarization and translation?
- a) GPT-2
- b) BERT
- c) CNN-RNN hybrid
- d) T5 (Text-to-Text Transfer Transformer)

18. Why do transformer models scale well to long sequences?
- a) Because of recursion
- b) Because of attention computed in parallel
- c) Because they skip training
- d) Because of LSTM units

19. What is one major limitation of basic transformers when working with very long texts?
- a) They cannot handle numbers
- b) Attention computation becomes expensive
- c) They ignore token order completely
- d) They cannot generate tokens

20. In translation tasks using transformers, what does the model learn?
- a) Mapping from source to target language
- b) Synonyms of words
- c) Token positions only
- d) How to perform arithmetic


