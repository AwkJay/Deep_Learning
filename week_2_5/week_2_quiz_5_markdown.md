# Week 2 Quiz 5: Autoencoders

---

## Questions

1. What type of learning is used in Autoencoders?
- a) Semi-Supervised Learning
- b) Reinforcement Learning
- c) Supervised Learning
- d) Unsupervised Learning

2. Which mathematical operation does the encoder part of an autoencoder mainly perform?
- a) Classification
- b) Compression / Dimensionality reduction
- c) Expansion of data
- d) Clustering

3. What is the typical shape of input and output in a basic autoencoder model?
- a) Output is randomly generated
- b) Input and output shapes are the same
- c) Input is larger than output
- d) Input and output shapes are always different

4. Which mathematical concept is used in the loss function of an autoencoder?
- a) Cross product
- b) Covariance
- c) Entropy
- d) Euclidean distance or Mean Squared Error (MSE)

5. In a neural network, what does a non-linear activation function (like ReLU or Sigmoid) help with?
- a) Speeding up matrix multiplication
- b) Reconstructing the output
- c) Introducing non-linearity to learn complex patterns
- d) Compressing the dataset

6. What is the purpose of the bottleneck layer in an autoencoder?
- a) It holds the compressed (latent) representation of the input
- b) It adds noise to the input
- c) It stores the model weights
- d) It is where the output is generated

7. What is the shape of the latent vector typically like compared to the input image?
- a) Random size
- b) Exactly the same
- c) Larger
- d) Smaller (lower-dimensional)

8. What does a denoising autoencoder learn to do?
- a) Ignore irrelevant features
- b) Classify images into categories
- c) Add more noise to input
- d) Convert noisy input into clean output

9. What is a key idea behind a Variational Autoencoder (VAE)?
- a) It maps inputs to a distribution (mean and variance) in latent space
- b) It doesn't use training data
- c) It encodes every input to a fixed value
- d) It skips decoding

10. Why is normalization (scaling input between 0 and 1) important before feeding data into an autoencoder?
- a) To make data colorful
- b) To speed up training and improve learning stability
- c) To create random noise
- d) To make the model memorize input better



