# Week 3 Quiz 1: Introduction to GAN

---

## Questions

1. What is the role of the generator in a GAN?
- a) Detects fake data
- b) Produces synthetic data
- c) Labels the training data
- d) Applies filters to input data

2. What type of model is the discriminator in GANs?
- a) Reinforcement agent
- b) Clustering algorithm
- c) Regression model
- d) Binary classifier

3. Why is GAN training described as a game?
- a) It runs in turns like a board game
- b) It has a single winner and loser
- c) Both networks are trying to solve puzzles
- d) The generator and discriminator compete with opposing goals

4. What does the generator take as input?
- a) Labeled datasets
- b) Class probabilities
- c) Real images
- d) Random noise vector

5. Which of the following best describes the output of the discriminator?
- a) A feature map
- b) A label from 0 to 9
- c) A probability between 0 and 1
- d) A noise vector

6. What is the meaning of "adversarial" in GANs?
- a) The model is adversarial to the user
- b) It avoids adversarial attacks
- c) It ignores the loss function
- d) The model is based on adversaries in game theory

7. What kind of loss is typically used in training GANs?
- a) Log-cosh loss
- b) Hinge loss
- c) Cross-entropy loss
- d) Mean squared error

8. What is mode collapse in GANs?
- a) When the generator starts producing noise only
- b) When the generator produces limited diversity in outputs
- c) When the loss becomes zero
- d) When the discriminator becomes a generator

9. What happens when the discriminator becomes too good too early?
- a) GAN converges faster
- b) Generator training speeds up
- c) Discriminator starts generating images
- d) Generator gradients vanish

10. How does the generator receive feedback during training?
- a) Via gradients from the discriminator
- b) From real image labels
- c) From evaluation metrics
- d) Through pixel-wise loss

11. Which is a popular GAN variant for generating high-quality images?
- a) AutoGAN
- b) GAN-CNN
- c) LSTM-GAN
- d) DCGAN

12. What is the main difference between a Conditional GAN and a Vanilla GAN?
- a) Conditional GAN uses LSTM layers
- b) Vanilla GANs are used for videos
- c) Conditional GAN is unsupervised
- d) Conditional GAN adds input labels to guide output

13. In image-to-image translation, what is the GAN trying to learn?
- a) Bounding boxes
- b) The number of classes
- c) A mapping between input and target image domains
- d) How to count objects in images

14. Which of the following is an application of GANs?
- a) Image classification
- b) Object detection
- c) Sorting data
- d) Data generation

15. Which is a method to stabilize GAN training?
- a) Label smoothing
- b) Ignoring overfitting
- c) Using only generator loss
- d) Decreasing batch size

16. What kind of data does the discriminator learn from?
- a) Only labels
- b) Only fake data
- c) Only noise
- d) Both real and fake data

17. What happens when GAN training is successful?
- a) Generator starts producing exact real images
- b) Generator fools the discriminator perfectly
- c) Discriminator always wins
- d) Training loss becomes zero

18. How many neural networks are present in a typical GAN architecture?
- a) 3
- b) 1
- c) 4
- d) 2

19. What kind of learning are GANs usually associated with?
- a) Reinforcement Learning
- b) Supervised Learning
- c) Self-supervised Learning
- d) Unsupervised Learning

20. What is the purpose of noise input in the generator component of a GAN?
- a) None of the above
- b) To control the learning rate during training
- c) To adjust the weights and biases of the generator
- d) To randomize the generation process and introduce variations



